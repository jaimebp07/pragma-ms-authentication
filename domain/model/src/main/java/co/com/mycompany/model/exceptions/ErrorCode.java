package co.com.mycompany.model.exceptions;

public enum ErrorCode {
    VALIDATION_ERROR,
    EMAIL_ALREADY_EXISTS,
    USER_NOT_FOUND,
    DB_ERROR,
    INTERNAL_ERROR,
    INVALID_ARGUMENT,
    VALUE_REQUIRED,
    INVALID_FORMAT,
    INVALID_CREDENTIALS
}

